

 
<div class="col-md-8 blog-main">
      <h3 class="pb-4 mb-4 font-italic border-bottom">Are you lost too?</h3>
  <% if logged_in?(:site_admin)%>
      <div>
   <%=link_to 'Create a new blog post', new_blog_path %>
   </div>
<%end%>
 
    <%= render @blogs.reverse_order %>
    <%= paginate @blogs%>

    </div>
